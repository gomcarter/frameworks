package com.gomcarter.frameworks.mybatis.annotation;

/**
 * @author gomcarter
 */
public enum JoinType {
    /**
     * INNER JOIN
     */
    INNER,
    /**
     * LEFT JOIN
     */
    LEFT,
    /**
     * RIGHT JOIN
     */
    RIGHT
}
